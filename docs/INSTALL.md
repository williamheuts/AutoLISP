# Installatie

1. Kopieer de map `ASME-Flange-Generator` naar een vaste locatie.
2. Voeg het pad toe aan AutoCAD's support paths.
3. Laad `package.lsp` via:
   - `APPLOAD`, of
   - `(load "C:/pad/ASME-Flange-Generator/package.lsp")`
4. Start het commando:
   - `FLANGEGEN`